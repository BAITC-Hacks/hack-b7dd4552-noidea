DATA_DIR=./data/workspace
PORT=8028
# Время CSV пока не подтверждено. Не подставлять часовой пояс автоматически.
# Для Docker данные всегда сохраняются в томе /app/data.

# Серверный ключ; используется только при явном запуске агента.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
